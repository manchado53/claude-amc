# Machine: Laptop (Windows)

This file holds laptop-only **environment** facts that have no single repo to live in.
Per-project context lives in each project's own CLAUDE.md (auto-loads in that directory).

Nothing machine-specific yet — add laptop-only env facts here as they come up
(e.g. local tool paths, OS quirks, environment managers).
